name: worduno
description: Destination vocabulary learning app — flashcard, exam, and AI coach.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.5
  get_it: ^8.0.3
  dio: ^5.8.0
  sqflite: ^2.4.2
  path: ^1.9.1
  flutter_tts: ^4.2.2
  sqflite_common_ffi: ^2.4.0+3
  sqflite_common_ffi_web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
